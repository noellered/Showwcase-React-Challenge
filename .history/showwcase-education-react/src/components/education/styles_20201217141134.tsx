import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
    root: {
        height: '75vh',
        overflow: 'auto'
    },
    educationCard: {
        padding: '2rem',
        margin: '1rem'
    }
})