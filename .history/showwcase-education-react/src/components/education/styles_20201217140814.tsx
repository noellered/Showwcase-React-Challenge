import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
    root: {
        maxHeight: '70vh',
        over
    },
    educationCard: {
        padding: '2rem'
    }
})