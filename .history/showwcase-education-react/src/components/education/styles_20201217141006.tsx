import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
    root: {
        maxHeight: '75vh',
        overflow: 'auto'
    },
    educationCard: {
        padding: '2rem'
    }
})