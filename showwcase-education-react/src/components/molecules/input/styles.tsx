import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
    input: {
        minWidth: '40%',
        marginBottom: '.5rem'
    },
    inputGroup: {
        display: 'flex',
        flexDirection: 'row'
    },
    mr: {
        marginRight: '.5rem'
    },

    label: {
        display: 'inline-block'
    },
})